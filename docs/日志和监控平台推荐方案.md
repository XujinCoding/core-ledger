# 日志分析和监控平台推荐方案

**适用场景**: 服务器资源受限环境
**原则**: 免费开源、轻量级、易部署、功能完善

---

## 一、推荐方案总览

### 🎯 最佳组合方案 (推荐)

```
┌─────────────────────────────────────────────┐
│           Grafana (统一可视化平台)            │
├─────────────────────────────────────────────┤
│  Loki (日志)  │  Prometheus (监控)           │
├─────────────────────────────────────────────┤
│  Promtail     │  Spring Boot Actuator       │
│  (日志采集)    │  (应用指标)                  │
└─────────────────────────────────────────────┘
```

**优势**:
- ✅ **资源占用低**: Loki 比 ELK 节省 10 倍内存
- ✅ **统一界面**: 一个 Grafana 展示所有数据
- ✅ **易于部署**: Docker Compose 一键启动
- ✅ **完全免费**: 所有组件开源免费

**资源需求**:
- **最低配置**: 2 核 CPU + 4GB 内存
- **推荐配置**: 4 核 CPU + 8GB 内存

---

## 二、日志分析平台

### 方案 1: Loki + Promtail + Grafana ⭐⭐⭐⭐⭐ (强烈推荐)

#### 2.1 方案介绍

**Loki**: 由 Grafana Labs 开发的轻量级日志聚合系统
- 灵感来自 Prometheus，但专注于日志
- 只索引标签，不索引日志内容（节省存储和内存）
- 使用 LogQL 查询语言（类似 PromQL）

**Promtail**: 日志采集代理
- 自动发现日志文件
- 支持多种日志格式
- 资源占用极低

**Grafana**: 可视化平台
- 统一展示日志和监控
- 强大的查询和过滤功能
- 支持告警

#### 2.2 资源占用

| 组件 | CPU | 内存 | 磁盘 |
|------|-----|------|------|
| Loki | 0.5 核 | 512MB | 10GB+ |
| Promtail | 0.1 核 | 128MB | - |
| Grafana | 0.2 核 | 256MB | 1GB |
| **总计** | **0.8 核** | **896MB** | **11GB+** |

#### 2.3 部署方式

**Docker Compose 部署** (推荐):

```yaml
# docker-compose-logging.yml
version: '3.8'

services:
  loki:
    image: grafana/loki:2.9.3
    container_name: loki
    ports:
      - "3100:3100"
    volumes:
      - ./loki-config.yml:/etc/loki/local-config.yaml
      - loki-data:/loki
    command: -config.file=/etc/loki/local-config.yaml
    restart: unless-stopped
    networks:
      - monitoring

  promtail:
    image: grafana/promtail:2.9.3
    container_name: promtail
    volumes:
      - ./promtail-config.yml:/etc/promtail/config.yml
      - /var/log:/var/log:ro
      - /mnt/h/core-ledger/core-ledger-service/logs:/app/logs:ro
    command: -config.file=/etc/promtail/config.yml
    restart: unless-stopped
    networks:
      - monitoring

  grafana:
    image: grafana/grafana:10.2.3
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin123
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana-data:/var/lib/grafana
    restart: unless-stopped
    networks:
      - monitoring

volumes:
  loki-data:
  grafana-data:

networks:
  monitoring:
    driver: bridge
```

#### 2.4 Loki 配置

```yaml
# loki-config.yml
auth_enabled: false

server:
  http_listen_port: 3100

ingester:
  lifecycler:
    address: 127.0.0.1
    ring:
      kvstore:
        store: inmemory
      replication_factor: 1
  chunk_idle_period: 5m
  chunk_retain_period: 30s

schema_config:
  configs:
    - from: 2023-01-01
      store: boltdb-shipper
      object_store: filesystem
      schema: v11
      index:
        prefix: index_
        period: 24h

storage_config:
  boltdb_shipper:
    active_index_directory: /loki/index
    cache_location: /loki/cache
    shared_store: filesystem
  filesystem:
    directory: /loki/chunks

limits_config:
  enforce_metric_name: false
  reject_old_samples: true
  reject_old_samples_max_age: 168h
  ingestion_rate_mb: 10
  ingestion_burst_size_mb: 20

chunk_store_config:
  max_look_back_period: 720h

table_manager:
  retention_deletes_enabled: true
  retention_period: 720h
```

#### 2.5 Promtail 配置

```yaml
# promtail-config.yml
server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  # Spring Boot 应用日志
  - job_name: spring-boot
    static_configs:
      - targets:
          - localhost
        labels:
          job: core-ledger
          app: core-ledger-service
          __path__: /app/logs/*.log
    pipeline_stages:
      - multiline:
          firstline: '^\d{4}-\d{2}-\d{2}'
      - regex:
          expression: '^(?P<timestamp>\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}\.\d{3})\s+(?P<level>\w+)\s+(?P<thread>\[.*?\])\s+(?P<logger>[\w\.]+)\s+:\s+(?P<message>.*)'
      - labels:
          level:
          logger:
      - timestamp:
          source: timestamp
          format: '2006-01-02 15:04:05.000'

  # 系统日志
  - job_name: system
    static_configs:
      - targets:
          - localhost
        labels:
          job: system
          __path__: /var/log/*.log
```

#### 2.6 Spring Boot 日志配置

```xml
<!-- logback-spring.xml -->
<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="LOG_PATH" value="logs"/>
    <property name="LOG_FILE" value="core-ledger"/>

    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %5p [%t] %logger{36} : %m%n</pattern>
        </encoder>
    </appender>

    <!-- 文件输出 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE}.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %5p [%t] %logger{36} : %m%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${LOG_FILE}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>100MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- 错误日志单独输出 -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE}-error.log</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %5p [%t] %logger{36} : %m%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${LOG_FILE}-error-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>
</configuration>
```

#### 2.7 使用示例

**LogQL 查询示例**:

```logql
# 查询所有错误日志
{app="core-ledger-service"} |= "ERROR"

# 查询特定类的日志
{app="core-ledger-service", logger="com.coreledger.service.LedgerService"}

# 查询包含异常的日志
{app="core-ledger-service"} |~ "Exception|Error"

# 统计错误数量
sum(count_over_time({app="core-ledger-service"} |= "ERROR" [5m]))

# 查询慢查询
{app="core-ledger-service"} |= "Hibernate" | regexp "(?P<time>\\d+)ms" | time > 1000
```

---

### 方案 2: ELK Stack (不推荐，资源占用大)

**组件**: Elasticsearch + Logstash + Kibana

**资源需求**:
- CPU: 4 核+
- 内存: 8GB+
- 磁盘: 50GB+

**不推荐原因**:
- ❌ Elasticsearch 内存占用大 (至少 2GB)
- ❌ Logstash 资源消耗高
- ❌ 部署和维护复杂
- ❌ 不适合资源受限环境

---

## 三、监控平台

### 方案 1: Prometheus + Grafana ⭐⭐⭐⭐⭐ (强烈推荐)

#### 3.1 方案介绍

**Prometheus**: 开源监控和告警系统
- 时序数据库
- 强大的查询语言 (PromQL)
- 服务发现和自动采集
- 内置告警管理

**Grafana**: 可视化平台
- 与 Loki 共用同一个 Grafana 实例
- 丰富的仪表板模板
- 支持多种数据源

**Spring Boot Actuator**: 应用监控端点
- 暴露应用指标
- 健康检查
- 性能监控

#### 3.2 资源占用

| 组件 | CPU | 内存 | 磁盘 |
|------|-----|------|------|
| Prometheus | 0.5 核 | 512MB | 10GB+ |
| Node Exporter | 0.1 核 | 64MB | - |
| **总计** | **0.6 核** | **576MB** | **10GB+** |

#### 3.3 部署方式

```yaml
# docker-compose-monitoring.yml
version: '3.8'

services:
  prometheus:
    image: prom/prometheus:v2.48.0
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
    restart: unless-stopped
    networks:
      - monitoring

  node-exporter:
    image: prom/node-exporter:v1.7.0
    container_name: node-exporter
    ports:
      - "9100:9100"
    restart: unless-stopped
    networks:
      - monitoring

volumes:
  prometheus-data:

networks:
  monitoring:
    driver: bridge
```

#### 3.4 Prometheus 配置

```yaml
# prometheus.yml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

# 告警规则
rule_files:
  - 'alerts.yml'

# 采集配置
scrape_configs:
  # Prometheus 自身监控
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # 服务器监控
  - job_name: 'node'
    static_configs:
      - targets: ['node-exporter:9100']

  # Spring Boot 应用监控
  - job_name: 'spring-boot'
    metrics_path: '/code-ledger/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']
        labels:
          application: 'core-ledger-service'

  # MySQL 监控 (可选)
  - job_name: 'mysql'
    static_configs:
      - targets: ['mysqld-exporter:9104']

  # Redis 监控 (可选)
  - job_name: 'redis'
    static_configs:
      - targets: ['redis-exporter:9121']
```

#### 3.5 告警规则

```yaml
# alerts.yml
groups:
  - name: application_alerts
    interval: 30s
    rules:
      # 应用宕机告警
      - alert: ApplicationDown
        expr: up{job="spring-boot"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "应用 {{ $labels.application }} 已宕机"
          description: "应用已经宕机超过 1 分钟"

      # CPU 使用率告警
      - alert: HighCPUUsage
        expr: 100 - (avg by(instance) (irate(node_cpu_seconds_total{mode="idle"}[5m])) * 100) > 80
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "CPU 使用率过高"
          description: "CPU 使用率超过 80%，当前值: {{ $value }}%"

      # 内存使用率告警
      - alert: HighMemoryUsage
        expr: (1 - (node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes)) * 100 > 85
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "内存使用率过高"
          description: "内存使用率超过 85%，当前值: {{ $value }}%"

      # 磁盘使用率告警
      - alert: HighDiskUsage
        expr: (1 - (node_filesystem_avail_bytes{fstype!~"tmpfs|fuse.lxcfs"} / node_filesystem_size_bytes)) * 100 > 85
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "磁盘使用率过高"
          description: "磁盘 {{ $labels.mountpoint }} 使用率超过 85%"

      # HTTP 错误率告警
      - alert: HighErrorRate
        expr: rate(http_server_requests_seconds_count{status=~"5.."}[5m]) > 0.05
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "HTTP 5xx 错误率过高"
          description: "5xx 错误率超过 5%"

      # 慢查询告警
      - alert: SlowQueries
        expr: histogram_quantile(0.95, rate(http_server_requests_seconds_bucket[5m])) > 1
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "接口响应时间过长"
          description: "95% 的请求响应时间超过 1 秒"
```

#### 3.6 Spring Boot 集成

**1. 添加依赖**:

```xml
<!-- pom.xml -->
<dependencies>
    <!-- Spring Boot Actuator -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <!-- Micrometer Prometheus -->
    <dependency>
        <groupId>io.micrometer</groupId>
        <artifactId>micrometer-registry-prometheus</artifactId>
    </dependency>
</dependencies>
```

**2. 配置 application.yml**:

```yaml
# application.yml
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
      base-path: /actuator
  endpoint:
    health:
      show-details: always
    metrics:
      enabled: true
    prometheus:
      enabled: true
  metrics:
    export:
      prometheus:
        enabled: true
    tags:
      application: ${spring.application.name}
    distribution:
      percentiles-histogram:
        http.server.requests: true
```

**3. 自定义指标**:

```java
// CustomMetrics.java
@Component
public class CustomMetrics {
    private final MeterRegistry meterRegistry;
    private final Counter ledgerCreatedCounter;
    private final Timer ledgerProcessingTimer;

    public CustomMetrics(MeterRegistry meterRegistry) {
        this.meterRegistry = meterRegistry;

        // 账单创建计数器
        this.ledgerCreatedCounter = Counter.builder("ledger.created")
            .description("账单创建总数")
            .tag("type", "business")
            .register(meterRegistry);

        // 账单处理时间
        this.ledgerProcessingTimer = Timer.builder("ledger.processing.time")
            .description("账单处理时间")
            .register(meterRegistry);
    }

    public void incrementLedgerCreated() {
        ledgerCreatedCounter.increment();
    }

    public void recordLedgerProcessingTime(long milliseconds) {
        ledgerProcessingTimer.record(milliseconds, TimeUnit.MILLISECONDS);
    }
}

// 在 Service 中使用
@Service
@RequiredArgsConstructor
public class LedgerService {
    private final CustomMetrics customMetrics;

    @Transactional
    public LedgerVO createLedger(CreateLedgerDTO dto) {
        long startTime = System.currentTimeMillis();
        try {
            // 业务逻辑
            Ledger ledger = // ...
            customMetrics.incrementLedgerCreated();
            return // ...
        } finally {
            customMetrics.recordLedgerProcessingTime(
                System.currentTimeMillis() - startTime
            );
        }
    }
}
```

#### 3.7 Grafana 仪表板

**导入预制仪表板**:

1. **Spring Boot 2.1 Statistics**: Dashboard ID `10280`
2. **JVM (Micrometer)**: Dashboard ID `4701`
3. **Node Exporter Full**: Dashboard ID `1860`

**自定义仪表板示例**:

```json
{
  "dashboard": {
    "title": "Core Ledger 监控",
    "panels": [
      {
        "title": "QPS (每秒请求数)",
        "targets": [
          {
            "expr": "rate(http_server_requests_seconds_count{application=\"core-ledger-service\"}[1m])"
          }
        ]
      },
      {
        "title": "响应时间 (P95)",
        "targets": [
          {
            "expr": "histogram_quantile(0.95, rate(http_server_requests_seconds_bucket{application=\"core-ledger-service\"}[5m]))"
          }
        ]
      },
      {
        "title": "错误率",
        "targets": [
          {
            "expr": "rate(http_server_requests_seconds_count{application=\"core-ledger-service\",status=~\"5..\"}[5m])"
          }
        ]
      },
      {
        "title": "JVM 堆内存使用",
        "targets": [
          {
            "expr": "jvm_memory_used_bytes{application=\"core-ledger-service\",area=\"heap\"}"
          }
        ]
      },
      {
        "title": "数据库连接池",
        "targets": [
          {
            "expr": "hikaricp_connections_active{application=\"core-ledger-service\"}"
          }
        ]
      },
      {
        "title": "账单创建数",
        "targets": [
          {
            "expr": "rate(ledger_created_total[5m])"
          }
        ]
      }
    ]
  }
}
```

---

### 方案 2: Zabbix (备选方案)

**适用场景**: 需要传统企业级监控的场景

**优点**:
- ✅ 功能强大，支持多种监控方式
- ✅ 自带告警系统
- ✅ 支持自动发现

**缺点**:
- ❌ 部署复杂 (需要 PHP + MySQL)
- ❌ 界面较老旧
- ❌ 资源占用较大

**不推荐原因**: 对于现代微服务应用，Prometheus + Grafana 更合适

---

## 四、完整部署方案

### 4.1 统一 Docker Compose 配置

```yaml
# docker-compose-observability.yml
version: '3.8'

services:
  # 日志系统
  loki:
    image: grafana/loki:2.9.3
    container_name: loki
    ports:
      - "3100:3100"
    volumes:
      - ./config/loki-config.yml:/etc/loki/local-config.yaml
      - loki-data:/loki
    command: -config.file=/etc/loki/local-config.yaml
    restart: unless-stopped
    networks:
      - monitoring

  promtail:
    image: grafana/promtail:2.9.3
    container_name: promtail
    volumes:
      - ./config/promtail-config.yml:/etc/promtail/config.yml
      - /var/log:/var/log:ro
      - /mnt/h/core-ledger/core-ledger-service/logs:/app/logs:ro
    command: -config.file=/etc/promtail/config.yml
    restart: unless-stopped
    networks:
      - monitoring
    depends_on:
      - loki

  # 监控系统
  prometheus:
    image: prom/prometheus:v2.48.0
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./config/prometheus.yml:/etc/prometheus/prometheus.yml
      - ./config/alerts.yml:/etc/prometheus/alerts.yml
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
      - '--web.enable-lifecycle'
    restart: unless-stopped
    networks:
      - monitoring

  node-exporter:
    image: prom/node-exporter:v1.7.0
    container_name: node-exporter
    ports:
      - "9100:9100"
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    restart: unless-stopped
    networks:
      - monitoring

  # 可视化平台
  grafana:
    image: grafana/grafana:10.2.3
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin123
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_ROOT_URL=http://localhost:3000
      - GF_INSTALL_PLUGINS=redis-datasource
    volumes:
      - grafana-data:/var/lib/grafana
      - ./config/grafana/provisioning:/etc/grafana/provisioning
    restart: unless-stopped
    networks:
      - monitoring
    depends_on:
      - prometheus
      - loki

  # 告警管理 (可选)
  alertmanager:
    image: prom/alertmanager:v0.26.0
    container_name: alertmanager
    ports:
      - "9093:9093"
    volumes:
      - ./config/alertmanager.yml:/etc/alertmanager/alertmanager.yml
      - alertmanager-data:/alertmanager
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
    restart: unless-stopped
    networks:
      - monitoring

volumes:
  loki-data:
  prometheus-data:
  grafana-data:
  alertmanager-data:

networks:
  monitoring:
    driver: bridge
```

### 4.2 Grafana 数据源自动配置

```yaml
# config/grafana/provisioning/datasources/datasources.yml
apiVersion: 1

datasources:
  # Prometheus 数据源
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

  # Loki 数据源
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    editable: true
```

### 4.3 告警管理配置

```yaml
# config/alertmanager.yml
global:
  resolve_timeout: 5m

# 告警路由
route:
  group_by: ['alertname', 'severity']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 12h
  receiver: 'default'
  routes:
    - match:
        severity: critical
      receiver: 'critical'
    - match:
        severity: warning
      receiver: 'warning'

# 接收器配置
receivers:
  - name: 'default'
    webhook_configs:
      - url: 'http://your-webhook-url/alert'

  - name: 'critical'
    # 企业微信告警
    wechat_configs:
      - corp_id: 'your-corp-id'
        to_user: '@all'
        agent_id: 'your-agent-id'
        api_secret: 'your-api-secret'
        message: '{{ template "wechat.default.message" . }}'

  - name: 'warning'
    # 邮件告警
    email_configs:
      - to: 'admin@example.com'
        from: 'alertmanager@example.com'
        smarthost: 'smtp.example.com:587'
        auth_username: 'alertmanager@example.com'
        auth_password: 'your-password'

# 抑制规则
inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'instance']
```

### 4.4 部署步骤

**1. 创建目录结构**:

```bash
mkdir -p monitoring/{config,config/grafana/provisioning/datasources}
cd monitoring
```

**2. 创建配置文件**:

```bash
# 复制上面的配置文件到对应目录
touch config/loki-config.yml
touch config/promtail-config.yml
touch config/prometheus.yml
touch config/alerts.yml
touch config/alertmanager.yml
touch config/grafana/provisioning/datasources/datasources.yml
```

**3. 启动服务**:

```bash
# 启动所有服务
docker-compose -f docker-compose-observability.yml up -d

# 查看服务状态
docker-compose -f docker-compose-observability.yml ps

# 查看日志
docker-compose -f docker-compose-observability.yml logs -f
```

**4. 访问界面**:

- **Grafana**: http://localhost:3000 (admin/admin123)
- **Prometheus**: http://localhost:9090
- **Alertmanager**: http://localhost:9093

**5. 配置 Grafana**:

```bash
# 登录 Grafana
# 1. 访问 http://localhost:3000
# 2. 使用 admin/admin123 登录
# 3. 导入仪表板:
#    - Configuration -> Data Sources (已自动配置)
#    - Dashboards -> Import -> 输入 Dashboard ID
#      - 10280 (Spring Boot)
#      - 4701 (JVM)
#      - 1860 (Node Exporter)
```

---

## 五、资源占用总结

### 5.1 完整方案资源需求

| 组件 | CPU | 内存 | 磁盘 | 端口 |
|------|-----|------|------|------|
| Loki | 0.5 核 | 512MB | 10GB | 3100 |
| Promtail | 0.1 核 | 128MB | - | 9080 |
| Prometheus | 0.5 核 | 512MB | 10GB | 9090 |
| Node Exporter | 0.1 核 | 64MB | - | 9100 |
| Grafana | 0.2 核 | 256MB | 1GB | 3000 |
| Alertmanager | 0.1 核 | 128MB | 1GB | 9093 |
| **总计** | **1.5 核** | **1.6GB** | **22GB** | - |

### 5.2 最小化方案 (不含 Alertmanager)

| 组件 | CPU | 内存 | 磁盘 |
|------|-----|------|------|
| **总计** | **1.4 核** | **1.47GB** | **21GB** |

**结论**: 在 2 核 4GB 服务器上可以运行，但建议 4 核 8GB 以获得更好性能。

---

## 六、使用指南

### 6.1 日志查询示例

**在 Grafana 中查询日志**:

```logql
# 1. 查看最近的错误日志
{app="core-ledger-service"} |= "ERROR" | line_format "{{.timestamp}} {{.level}} {{.message}}"

# 2. 查询特定用户的操作日志
{app="core-ledger-service"} |= "userId=12345"

# 3. 查询慢查询 (超过 1 秒)
{app="core-ledger-service"} |= "Hibernate" | regexp "(?P<time>\\d+)ms" | time > 1000

# 4. 统计每分钟错误数
sum(count_over_time({app="core-ledger-service"} |= "ERROR" [1m]))

# 5. 查询 API 调用日志
{app="core-ledger-service"} |= "POST /api/ledgers" | json | line_format "{{.method}} {{.uri}} {{.status}} {{.duration}}ms"
```

### 6.2 监控查询示例

**在 Grafana 中查询指标**:

```promql
# 1. QPS (每秒请求数)
rate(http_server_requests_seconds_count{application="core-ledger-service"}[1m])

# 2. 平均响应时间
rate(http_server_requests_seconds_sum{application="core-ledger-service"}[5m])
/
rate(http_server_requests_seconds_count{application="core-ledger-service"}[5m])

# 3. P95 响应时间
histogram_quantile(0.95,
  rate(http_server_requests_seconds_bucket{application="core-ledger-service"}[5m])
)

# 4. 错误率
rate(http_server_requests_seconds_count{application="core-ledger-service",status=~"5.."}[5m])
/
rate(http_server_requests_seconds_count{application="core-ledger-service"}[5m])

# 5. JVM 堆内存使用率
jvm_memory_used_bytes{application="core-ledger-service",area="heap"}
/
jvm_memory_max_bytes{application="core-ledger-service",area="heap"}
* 100

# 6. 数据库连接池使用情况
hikaricp_connections_active{application="core-ledger-service"}

# 7. CPU 使用率
100 - (avg by(instance) (irate(node_cpu_seconds_total{mode="idle"}[5m])) * 100)

# 8. 内存使用率
(1 - (node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes)) * 100

# 9. 磁盘使用率
(1 - (node_filesystem_avail_bytes / node_filesystem_size_bytes)) * 100

# 10. 自定义业务指标 - 账单创建速率
rate(ledger_created_total[5m])
```

### 6.3 告警配置示例

**配置企业微信告警**:

```yaml
# config/alertmanager.yml
receivers:
  - name: 'wechat'
    wechat_configs:
      - corp_id: 'ww1234567890abcdef'
        to_user: '@all'
        agent_id: '1000002'
        api_secret: 'your-secret-key'
        send_resolved: true
        message: |
          {{ range .Alerts }}
          告警名称: {{ .Labels.alertname }}
          告警级别: {{ .Labels.severity }}
          告警详情: {{ .Annotations.description }}
          开始时间: {{ .StartsAt.Format "2006-01-02 15:04:05" }}
          {{ end }}
```

---

## 七、维护和优化

### 7.1 日常维护

**1. 日志清理**:

```bash
# 清理 30 天前的日志
find /mnt/h/core-ledger/core-ledger-service/logs -name "*.log" -mtime +30 -delete

# 或在 Loki 配置中设置保留期
# limits_config:
#   retention_period: 720h  # 30 天
```

**2. 数据备份**:

```bash
# 备份 Prometheus 数据
docker exec prometheus tar czf /prometheus/backup-$(date +%Y%m%d).tar.gz /prometheus/data

# 备份 Grafana 配置
docker exec grafana tar czf /var/lib/grafana/backup-$(date +%Y%m%d).tar.gz /var/lib/grafana
```

**3. 性能优化**:

```yaml
# Loki 性能优化
limits_config:
  ingestion_rate_mb: 10        # 限制摄入速率
  ingestion_burst_size_mb: 20  # 突发大小
  max_streams_per_user: 10000  # 最大流数量
  max_query_length: 721h       # 最大查询时间范围

# Prometheus 性能优化
storage:
  tsdb:
    retention.time: 30d        # 数据保留 30 天
    retention.size: 10GB       # 最大存储 10GB
```

### 7.2 故障排查

**1. Loki 无法接收日志**:

```bash
# 检查 Promtail 状态
docker logs promtail

# 检查 Loki 状态
docker logs loki

# 测试连接
curl http://localhost:3100/ready
```

**2. Prometheus 无法采集指标**:

```bash
# 检查 targets 状态
curl http://localhost:9090/api/v1/targets

# 检查应用 actuator 端点
curl http://localhost:8080/code-ledger/actuator/prometheus
```

**3. Grafana 无法显示数据**:

```bash
# 检查数据源连接
# Grafana UI -> Configuration -> Data Sources -> Test

# 检查查询语法
# 使用 Explore 功能测试查询
```

---

## 八、成本分析

### 8.1 资源成本

**服务器配置**: 4 核 8GB 内存 50GB 磁盘

| 云服务商 | 配置 | 月费用 |
|---------|------|--------|
| 阿里云 | 4C8G | ¥200-300 |
| 腾讯云 | 4C8G | ¥200-300 |
| 华为云 | 4C8G | ¥200-300 |

**软件成本**: ¥0 (完全开源免费)

### 8.2 与商业方案对比

| 方案 | 月费用 | 功能 |
|------|--------|------|
| **开源方案** | ¥0 | 完整功能 |
| 阿里云 SLS | ¥500+ | 日志分析 |
| 腾讯云 CLS | ¥500+ | 日志分析 |
| Datadog | $15/host | 监控+日志 |
| New Relic | $99/月 | APM |

**结论**: 开源方案可节省 **¥500-1000/月**

---

## 九、总结

### 9.1 推荐方案

**最佳组合**: Loki + Promtail + Prometheus + Grafana + Alertmanager

**优势**:
- ✅ 完全免费开源
- ✅ 资源占用低 (1.5 核 + 1.6GB 内存)
- ✅ 功能完善 (日志 + 监控 + 告警)
- ✅ 统一界面 (Grafana)
- ✅ 易于部署 (Docker Compose)
- ✅ 社区活跃，文档丰富

**适用场景**:
- ✅ 中小型应用
- ✅ 资源受限环境
- ✅ 快速部署需求
- ✅ 预算有限项目

### 9.2 实施建议

**第一阶段** (1 周):
1. 部署 Loki + Promtail + Grafana (日志系统)
2. 配置 Spring Boot 日志输出
3. 创建基础日志查询面板

**第二阶段** (1 周):
1. 部署 Prometheus + Node Exporter (监控系统)
2. 集成 Spring Boot Actuator
3. 导入预制监控面板

**第三阶段** (1 周):
1. 配置告警规则
2. 集成 Alertmanager
3. 配置企业微信/邮件通知

**第四阶段** (持续):
1. 优化查询和面板
2. 添加自定义业务指标
3. 性能调优

---

**文档版本**: v1.0
**最后更新**: 2026-02-04
**下一步**: 开始部署实施

